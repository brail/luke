# Template ADR/README della skill luke-docs.
#
# Citano path che per costruzione non esistono (`docs/decisions/NNNN-*`), quindi
# ogni riga porta un marker `skill-check-ignore` per `check-skill-integrity.ts`:
# 91 marker su ~116 righe in adr-rules.md, dentro i code fence compresi.
#
# Prettier non riesce a delimitare i blocchi così marcati e reagisce promuovendo
# il fence da 3 a 4 backtick — un cambio di contenuto in un file che è un
# template, non una riformattazione.
#
# Cerotto, non cura: la correzione vera è far saltare a check-skill-integrity.ts
# i blocchi fenced, così i marker a raffica spariscono e prettier torna a
# funzionare da solo. Il suo stesso docblock avverte contro i marker a raffica.
.claude/skills/luke-docs/references/
